function execute()
    -- Listen for OS messages, with a timeout
    local timerID = os.startTimer(0.1) -- .1 second
    local event, p1, p2, p3, p4, p5 = os.pullEvent()
    
    -- TODO: Handle different kinds of messages here, such as incoming swarmnet events.
    
    -- print("IDLE: Handled event [" .. event .. "]")
end