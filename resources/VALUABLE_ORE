function matchName(itemName)
    --[[ For now we're just going to detect the things we don't want. In time, this should
        become an inclusive list of materials (particularly ores) which we definitely want
        to extract, ignoring all else. ]]--
    if itemName == "minecraft:dirt" then return false end
    if itemName == "minecraft:gravel" then return false end
    if itemName == "minecraft:stone" then return false end
    if itemName == "minecraft:cobblestone" then return false end
    if itemName == "minecraft:sand" then return false end
    if itemName == "IC2:blockBasalt" then return false end
    if itemName == "bluepower:basalt" then return false end
    if itemName == "bluepower:basalt_cobble" then return false end
    if itemName == "bluepower:marble" then return false end
    if itemName == "Railcraft:tile.railcraft.cube" then return false end -- Quarried Stone (metadata=7)
    
    return false
end